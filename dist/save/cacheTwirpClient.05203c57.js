require("./core.da66a1bd.js");
require("./user-agent.bdfa9d84.js");
require("./errors.dc1ca1e5.js");
require("./config.8782b754.js");
require("./cacheUtils.dc94c7f3.js");
require("./auth.808e04d8.js");
require("./http-client.a6473211.js");
require("./cache.twirp.4b4a38dd.js");


      var $parcel$global = globalThis;
    
var $parcel$modules = {};
var $parcel$inits = {};

var parcelRequire = $parcel$global["parcelRequire94c2"];

if (parcelRequire == null) {
  parcelRequire = function(id) {
    if (id in $parcel$modules) {
      return $parcel$modules[id].exports;
    }
    if (id in $parcel$inits) {
      var init = $parcel$inits[id];
      delete $parcel$inits[id];
      var module = {id: id, exports: {}};
      $parcel$modules[id] = module;
      init.call(module.exports, module, module.exports);
      return module.exports;
    }
    var err = new Error("Cannot find module '" + id + "'");
    err.code = 'MODULE_NOT_FOUND';
    throw err;
  };

  parcelRequire.register = function register(id, init) {
    $parcel$inits[id] = init;
  };

  $parcel$global["parcelRequire94c2"] = parcelRequire;
}

var parcelRegister = parcelRequire.register;
parcelRegister("1Pn8v", function(module, exports) {
"use strict";
var $154c803b9435c9e7$var$__awaiter = module.exports && module.exports.__awaiter || function(thisArg, _arguments, P, generator) {
    function adopt(value) {
        return value instanceof P ? value : new P(function(resolve) {
            resolve(value);
        });
    }
    return new (P || (P = Promise))(function(resolve, reject) {
        function fulfilled(value) {
            try {
                step(generator.next(value));
            } catch (e) {
                reject(e);
            }
        }
        function rejected(value) {
            try {
                step(generator["throw"](value));
            } catch (e) {
                reject(e);
            }
        }
        function step(result) {
            result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
        }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(module.exports, "__esModule", {
    value: true
});
module.exports.internalCacheTwirpClient = void 0;

var $1irLk = parcelRequire("1irLk");

var $eJQr0 = parcelRequire("eJQr0");

var $6BSn8 = parcelRequire("6BSn8");

var $dBSoB = parcelRequire("dBSoB");

var $5xPwS = parcelRequire("5xPwS");

var $2Xj8U = parcelRequire("2Xj8U");

var $jmQwl = parcelRequire("jmQwl");

var $hNSY8 = parcelRequire("hNSY8");
/**
 * This class is a wrapper around the CacheServiceClientJSON class generated by Twirp.
 *
 * It adds retry logic to the request method, which is not present in the generated client.
 *
 * This class is used to interact with cache service v2.
 */ class $154c803b9435c9e7$var$CacheServiceClient {
    constructor(userAgent, maxAttempts, baseRetryIntervalMilliseconds, retryMultiplier){
        this.maxAttempts = 5;
        this.baseRetryIntervalMilliseconds = 3000;
        this.retryMultiplier = 1.5;
        const token = (0, $5xPwS.getRuntimeToken)();
        this.baseUrl = (0, $dBSoB.getCacheServiceURL)();
        if (maxAttempts) this.maxAttempts = maxAttempts;
        if (baseRetryIntervalMilliseconds) this.baseRetryIntervalMilliseconds = baseRetryIntervalMilliseconds;
        if (retryMultiplier) this.retryMultiplier = retryMultiplier;
        this.httpClient = new $jmQwl.HttpClient(userAgent, [
            new $2Xj8U.BearerCredentialHandler(token)
        ]);
    }
    // This function satisfies the Rpc interface. It is compatible with the JSON
    // JSON generated client.
    request(service, method, contentType, data) {
        return $154c803b9435c9e7$var$__awaiter(this, void 0, void 0, function*() {
            const url = new URL(`/twirp/${service}/${method}`, this.baseUrl).href;
            (0, $1irLk.debug)(`[Request] ${method} ${url}`);
            const headers = {
                'Content-Type': contentType
            };
            try {
                const { body: body } = yield this.retryableRequest(()=>$154c803b9435c9e7$var$__awaiter(this, void 0, void 0, function*() {
                        return this.httpClient.post(url, JSON.stringify(data), headers);
                    }));
                return body;
            } catch (error) {
                throw new Error(`Failed to ${method}: ${error.message}`);
            }
        });
    }
    retryableRequest(operation) {
        return $154c803b9435c9e7$var$__awaiter(this, void 0, void 0, function*() {
            let attempt = 0;
            let errorMessage = '';
            let rawBody = '';
            while(attempt < this.maxAttempts){
                let isRetryable = false;
                try {
                    const response = yield operation();
                    const statusCode = response.message.statusCode;
                    rawBody = yield response.readBody();
                    (0, $1irLk.debug)(`[Response] - ${response.message.statusCode}`);
                    (0, $1irLk.debug)(`Headers: ${JSON.stringify(response.message.headers, null, 2)}`);
                    const body = JSON.parse(rawBody);
                    (0, $1irLk.debug)(`Body: ${JSON.stringify(body, null, 2)}`);
                    if (this.isSuccessStatusCode(statusCode)) return {
                        response: response,
                        body: body
                    };
                    isRetryable = this.isRetryableHttpStatusCode(statusCode);
                    errorMessage = `Failed request: (${statusCode}) ${response.message.statusMessage}`;
                    if (body.msg) {
                        if ($6BSn8.UsageError.isUsageErrorMessage(body.msg)) throw new $6BSn8.UsageError();
                        errorMessage = `${errorMessage}: ${body.msg}`;
                    }
                } catch (error) {
                    if (error instanceof SyntaxError) (0, $1irLk.debug)(`Raw Body: ${rawBody}`);
                    if (error instanceof $6BSn8.UsageError) throw error;
                    if ($6BSn8.NetworkError.isNetworkErrorCode(error === null || error === void 0 ? void 0 : error.code)) throw new $6BSn8.NetworkError(error === null || error === void 0 ? void 0 : error.code);
                    isRetryable = true;
                    errorMessage = error.message;
                }
                if (!isRetryable) throw new Error(`Received non-retryable error: ${errorMessage}`);
                if (attempt + 1 === this.maxAttempts) throw new Error(`Failed to make request after ${this.maxAttempts} attempts: ${errorMessage}`);
                const retryTimeMilliseconds = this.getExponentialRetryTimeMilliseconds(attempt);
                (0, $1irLk.info)(`Attempt ${attempt + 1} of ${this.maxAttempts} failed with error: ${errorMessage}. Retrying request in ${retryTimeMilliseconds} ms...`);
                yield this.sleep(retryTimeMilliseconds);
                attempt++;
            }
            throw new Error(`Request failed`);
        });
    }
    isSuccessStatusCode(statusCode) {
        if (!statusCode) return false;
        return statusCode >= 200 && statusCode < 300;
    }
    isRetryableHttpStatusCode(statusCode) {
        if (!statusCode) return false;
        const retryableStatusCodes = [
            $jmQwl.HttpCodes.BadGateway,
            $jmQwl.HttpCodes.GatewayTimeout,
            $jmQwl.HttpCodes.InternalServerError,
            $jmQwl.HttpCodes.ServiceUnavailable,
            $jmQwl.HttpCodes.TooManyRequests
        ];
        return retryableStatusCodes.includes(statusCode);
    }
    sleep(milliseconds) {
        return $154c803b9435c9e7$var$__awaiter(this, void 0, void 0, function*() {
            return new Promise((resolve)=>setTimeout(resolve, milliseconds));
        });
    }
    getExponentialRetryTimeMilliseconds(attempt) {
        if (attempt < 0) throw new Error('attempt should be a positive integer');
        if (attempt === 0) return this.baseRetryIntervalMilliseconds;
        const minTime = this.baseRetryIntervalMilliseconds * Math.pow(this.retryMultiplier, attempt);
        const maxTime = minTime * this.retryMultiplier;
        // returns a random number between minTime and maxTime (exclusive)
        return Math.trunc(Math.random() * (maxTime - minTime) + minTime);
    }
}
function $154c803b9435c9e7$var$internalCacheTwirpClient(options) {
    const client = new $154c803b9435c9e7$var$CacheServiceClient((0, $eJQr0.getUserAgentString)(), options === null || options === void 0 ? void 0 : options.maxAttempts, options === null || options === void 0 ? void 0 : options.retryIntervalMs, options === null || options === void 0 ? void 0 : options.retryMultiplier);
    return new $hNSY8.CacheServiceClientJSON(client);
}
module.exports.internalCacheTwirpClient = $154c803b9435c9e7$var$internalCacheTwirpClient;

});
parcelRegister("hNSY8", function(module, exports) {
module.exports = new URL("cache.twirp.4b4a38dd.js", "file:" + __filename).toString();

});



